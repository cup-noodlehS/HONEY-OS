export type SimulationProcess = {
  arrivalTime: number;
  burstTime: number;
  color: string;
  memorySize: number;
  priority: number;
  processId: string;
  status: string;
  waitingTime: number;
};
