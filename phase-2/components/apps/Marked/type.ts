type SimulationProcess = {
  arrivalTime: string;
  burstTime: number;
  memorySize: number;
  priority: number;
  processId: string;
  status: string;
  waitingTime: number;
  color: string;
};

export type SimulationProcess;
